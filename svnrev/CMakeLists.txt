CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(svnrev)
ADD_DEFINITIONS(-D_CRT_SECURE_NO_WARNINGS)
SET(src ${CMAKE_CURRENT_SOURCE_DIR})
SET(top ${src}/..)
INCLUDE_DIRECTORIES(${src} ${top})
ADD_EXECUTABLE(svnrev main.cpp)
